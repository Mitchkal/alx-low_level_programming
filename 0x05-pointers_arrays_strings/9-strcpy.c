#include "main.h"

/**
*_strcpy - copy string fromsource to destination
*@dest : The destination
*@src : the source
*Return: returns the pointer to the destination
*/

char *_strcpy(char *dest, char *src)
{

	strcpy(dest, src);

	return (dest);
}
